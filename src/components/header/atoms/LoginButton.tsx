import { LoginButtonWrapper } from "../../../styles/headerStyled/HeaderStyled";

const LoginButton = () => {
  return (
    <LoginButtonWrapper>
      <button>로그인</button>
    </LoginButtonWrapper>
  );
};

export default LoginButton;
